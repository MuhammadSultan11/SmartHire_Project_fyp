function redirectToSignup(userType) {
    if (userType === 'hr') {
      window.location.href = '/signup/'; // Replace with actual HR signup page link
    } else if (userType === 'candidate') {
      window.location.href = '/signup/'; // Replace with actual Candidate signup page link
    }
  }

  